{
  "name": "ur-app",
  "private": true,
  "scripts": {
    "dev": "npm run backend && electron ./frontend/electron/main.js",
    "electron": "cd frontend && electron .",
    "backend": "docker compose build api && docker compose up -d",
    "backend:down": "docker compose down",
    "backend:logs": "docker compose logs -f api"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "electron": "^31.0.0"
  },
  "dependencies": {
    "yazl": "^3.3.1"
  }
}
