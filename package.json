{
  "name": "ur-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n PY,EL \"cd backend_py && uvicorn app.main:app --host 127.0.0.1 --port 5178\" \"cd frontend && electron .\"",
    "electron": "cd frontend && electron ."
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "electron": "^31.0.0"
  }
}
